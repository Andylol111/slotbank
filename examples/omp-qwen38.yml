# Oh My Pi (OMP) → slotbank Anthropic Messages door.
# 4-bit + native MTP (trained K=3). Cool A/B 2026-08-31: MTP 13.47/9.95 tok/s
# vs DFlash2 K=8 11.76/9.10 (count / parse_iso_dates), same greedy prefix.
# Thinking + vision + --direct stay on the model.
#   PYTHONPATH=src .venv/bin/python -m slotbank.cli serve \
#     --model ~/Models/Qwen3.8-27B-4bit \
#     --draft ~/Models/Qwen3.8-27B-MTP-4bit \
#     --leave-free 6g --thinking --vision --direct
# Then copy this provider into ~/.omp/agent/models.yml.
providers:
  slotbank-qwen38-27b:
    type: anthropic
    base_url: http://127.0.0.1:8080
    model: Qwen3.8-27B-4bit
